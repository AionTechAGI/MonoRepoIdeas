# Repo docs

This directory holds repository-wide process docs for the monorepo.

## Current docs

- `repo-conventions.md` for naming and layout rules
- `git-workflow.md` for branches, commits, and pull request hygiene
- `decisions/0001-monorepo-layout.md` for the initial monorepo decision record
