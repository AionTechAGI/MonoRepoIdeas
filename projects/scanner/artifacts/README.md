# Scanner artifacts

Use this directory for generated reports and saved research outputs.

- Keep durable reports in `reports/`.
- Keep disposable outputs in `generated/` or `tmp/`.
- Do not place generated files in the project root.
