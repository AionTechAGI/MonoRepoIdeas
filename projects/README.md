# Projects

This directory contains product folders for `MonoRepoIdeas`.

## Rules

- Each product lives in `projects/<ascii-slug>/`.
- Each product should include its own `README.md` and `AGENTS.md`.
- Generated files belong in `artifacts/` or `outputs/`, not in the project root.

## Current products

- `scanner/`
- `trading_strategy_tester/`
